<UserControl x:Class="AydaMusavirlik.Desktop.Views.Payroll.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#252526">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Personel Kartlari" FontSize="24" Foreground="White" Margin="0,0,0,20"/>
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="txtSearch" Background="#3E3E42" Foreground="White" Padding="10,8" FontSize="14" BorderBrush="#555" BorderThickness="1"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Yeni Personel" Click="YeniPersonel_Click" Background="#007ACC" Foreground="White" Padding="15,8" Margin="5,0" FontSize="14" BorderThickness="0" Cursor="Hand"/>
                <Button Content="Yenile" Click="Yenile_Click" Background="#3E3E42" Foreground="White" Padding="15,8" Margin="5,0" FontSize="14" BorderThickness="1" BorderBrush="#555" Cursor="Hand"/>
            </StackPanel>
        </Grid>
        <DataGrid Grid.Row="2" x:Name="dgEmployees" AutoGenerateColumns="False" Background="#2D2D30" Foreground="White" BorderThickness="1" BorderBrush="#555" RowBackground="#2D2D30" AlternatingRowBackground="#333337" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#444" HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sicil No" Binding="{Binding EmployeeNumber}" Width="80"/>
                <DataGridTextColumn Header="Ad Soyad" Binding="{Binding FullName}" Width="180"/>
                <DataGridTextColumn Header="TC Kimlik No" Binding="{Binding TcKimlikNo}" Width="130"/>
                <DataGridTextColumn Header="Departman" Binding="{Binding Department}" Width="120"/>
                <DataGridTextColumn Header="Pozisyon" Binding="{Binding Position}" Width="120"/>
                <DataGridTextColumn Header="Ise Giris" Binding="{Binding HireDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Brut Maas" Binding="{Binding GrossSalary, StringFormat=N2}" Width="120"/>
                <DataGridTemplateColumn Header="Islemler" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Duzenle" Click="Duzenle_Click" Background="Transparent" Foreground="White" BorderThickness="0" Padding="5" Cursor="Hand"/>
                                <Button Content="Sil" Click="Sil_Click" Background="Transparent" Foreground="Red" BorderThickness="0" Padding="5" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="3" Background="#2D2D30" BorderBrush="#555" BorderThickness="1" Padding="15" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="txtRecordCount" Text="Toplam: 0 personel" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,30,0"/>
                    <TextBlock x:Name="txtActiveCount" Text="Aktif: 0" Foreground="LightGreen" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Toplam Brut Maas: " Foreground="Gray" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtTotalSalary" Text="0,00 TL" Foreground="#4CAF50" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>