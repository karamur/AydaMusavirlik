<UserControl x:Class="AydaMusavirlik.Desktop.Views.CompaniesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Firmalar" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Musteri firma kayitlari" Foreground="Gray" />
        </StackPanel>
        
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Firma ara..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,16,0" />
                
                <Button Grid.Column="1" 
                        Command="{Binding AddCompanyCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Content="Yeni Firma" />
            </Grid>
        </Border>
        
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Companies}"
                      SelectedItem="{Binding SelectedCompany}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Firma Adi" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Vergi No" Binding="{Binding TaxNumber}" Width="120" />
                    <DataGridTextColumn Header="Sirket Turu" Binding="{Binding CompanyType}" Width="120" />
                    <DataGridTextColumn Header="Il" Binding="{Binding City}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
