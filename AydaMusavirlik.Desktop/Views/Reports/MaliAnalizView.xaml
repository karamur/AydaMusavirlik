<UserControl x:Class="AydaMusavirlik.Desktop.Views.Reports.MaliAnalizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="#1E1E1E">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Baslik -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Mali Analiz Raporu" FontSize="26" Foreground="White" FontWeight="Bold"/>
            <TextBlock Text="Rasyo, DuPont, SWOT ve Trend analizleri" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Filtreler -->
        <Border Grid.Row="1" Background="#2D2D30" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Firma:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbFirma" Width="250" DisplayMemberPath="Name" Margin="0,0,20,0"/>
                <TextBlock Text="Yil:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbYil" Width="100" Margin="0,0,20,0"/>
                <Button x:Name="btnAnaliz" Content="Analiz Yap" Width="120" Click="BtnAnaliz_Click"
                        Background="#007ACC" Foreground="White"/>
                <Separator Width="20" Background="Transparent"/>
                <Button x:Name="btnExport" Content="PDF Export" Width="100" Click="BtnExport_Click"
                        Background="#4CAF50" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Tab Control -->
        <TabControl x:Name="tabAnaliz" Grid.Row="2" Background="#252526" BorderThickness="0">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="15,8"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- Rasyo Analizi -->
            <TabItem Header="?? Rasyo Analizi">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                    <StackPanel x:Name="pnlRasyo" Margin="20"/>
                </ScrollViewer>
            </TabItem>

            <!-- DuPont Analizi -->
            <TabItem Header="?? DuPont Analizi">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                    <StackPanel x:Name="pnlDuPont" Margin="20"/>
                </ScrollViewer>
            </TabItem>

            <!-- SWOT Analizi -->
            <TabItem Header="? SWOT Analizi">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                    <Grid x:Name="pnlSwot" Margin="20"/>
                </ScrollViewer>
            </TabItem>

            <!-- Trend Analizi -->
            <TabItem Header="?? Trend Analizi">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                    <StackPanel x:Name="pnlTrend" Margin="20"/>
                </ScrollViewer>
            </TabItem>

            <!-- Mali Saglik -->
            <TabItem Header="?? Mali Saglik">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                    <StackPanel x:Name="pnlSaglik" Margin="20"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>